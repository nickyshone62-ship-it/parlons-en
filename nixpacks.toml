[providers]
providers = ["node"]

[variables]
NIXPACKS_NODE_VERSION = "20"
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
